- job-template:
    name: 'image-ci-{suffix}'  # Runs on each node once
    builders:
    - shell: |
        # TODO(fejta): consider a stable tag instead of master
        git clone https://github.com/kubernetes/test-infra -b master
        './test-infra/jenkins/bootstrap.py' --job='{job-name}' --repo='{repo-name}' --branch='{branch}' --root="${{GOPATH}}/src"
    node: 'e2e'
    properties:
    - build-discarder:
        days-to-keep: 14
    wrappers:
    - e2e-credentials-binding
    - inject:
        properties-content: |
            GOROOT=/usr/local/go
            GOPATH=$WORKSPACE/go
            PATH=$PATH:$GOROOT/bin:$WORKSPACE/go/bin
    - workspace-cleanup:
        dirmatch: true
        exclude:
        - 'go/src/{repo-name}/.git/'
        - 'test-infra/.git/'
        external-deletion-command: 'sudo rm -rf %s'
    - timeout:
        timeout: 30
        fail: true
    triggers:
    - timed: '{frequency}'

- project:
    name: bootstrap-image-ci-jobs
    jobs:
    - 'image-ci-{suffix}'
    suffix:
    - e2e-images:
        branch: master
        job-name: image-ci-e2e-images
        frequency: '@daily'
        repo-name: 'k8s.io/test-infra'
