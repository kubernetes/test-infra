- job:
    name: 'clean-pr-node-e2e'
    description: 'Clean up leaked node e2e resources. Test owner: test-infra maintainers.'
    node: ''
    triggers:
        - timed: 'H H/3 * * *'
    properties:
        - build-discarder:
            days-to-keep: 14
    wrappers:
        - e2e-credentials-binding
        - timeout:
            timeout: 15
            fail: true
    builders:
        - shell: |
            git clone https://github.com/kubernetes/test-infra
            './test-infra/jenkins/bootstrap.py' --job=clean-pr-node-e2e --repo=kubernetes/test-infra --branch=master
