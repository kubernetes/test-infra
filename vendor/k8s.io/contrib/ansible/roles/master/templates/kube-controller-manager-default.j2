. {{ kube_config_dir }}/config
. {{ kube_config_dir }}/controller-manager

KUBE_CONTROLLER_MANAGER_OPTS="$KUBE_LOGTOSTDERR \
                              $KUBE_LOG_LEVEL \
                              $KUBE_MASTER \
                              $KUBE_CONTROLLER_MANAGER_ARGS"
