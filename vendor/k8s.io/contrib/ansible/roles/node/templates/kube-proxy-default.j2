. {{ kube_config_dir }}/config
. {{ kube_config_dir }}/proxy

KUBE_PROXY_OPTS="$KUBE_LOGTOSTDERR \
                 $KUBE_LOG_LEVEL \
                 $KUBE_MASTER \
                 $KUBE_PROXY_ARGS"
