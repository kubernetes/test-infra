[Service]
# Note: ExecStart= is required to clear the ExecStart in the unit file
ExecStart=
ExecStart={{ bin_dir }}/kube-proxy \
	    $KUBE_LOGTOSTDERR \
	    $KUBE_LOG_LEVEL \
	    $KUBE_MASTER \
	    $KUBE_PROXY_ARGS
