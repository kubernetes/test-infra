apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress
spec:
  rules:
  - http:
      paths:
      - path: /fakeghserver
        backend:
          serviceName: fakeghserver
          servicePort: 80
