apiVersion: v1
kind: Service
metadata:
  namespace: default
  labels:
    app: ghproxy
  name: ghproxy
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8888
  selector:
    app: ghproxy
  type: NodePort # TODO(fejta): remove this?
