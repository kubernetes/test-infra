postsubmits:
  kubernetes/minikube:
    - name: post-kubernetes-bootcamp-image
      cluster: k8s-infra-prow-build-trusted
      annotations:
        testgrid-dashboards: minikube-images, sig-k8s-infra-gcb
      decorate: true
      branches:
        - ^images$
      spec:
        serviceAccountName: gcb-builder
        containers:
          - image: gcr.io/k8s-staging-test-infra/image-builder:v20251215-d7853fe2a6
            command:
              - /run.sh
            args:
              - --project=k8s-staging-images
              - --scratch-bucket=gs://k8s-staging-images-gcb
              - --env-passthrough=PULL_BASE_REF
              - --gcb-config=hack/prow/image/kubernetes-bootcamp/cloudbuild.yaml
              - .
            env:
              - name: LOG_TO_STDOUT
                value: "y"
    - name: post-minikube-gvisor-addon-image
      cluster: k8s-infra-prow-build-trusted
      annotations:
        testgrid-dashboards: minikube-images, sig-k8s-infra-gcb
      decorate: true
      branches:
        - ^images$
      spec:
        serviceAccountName: gcb-builder
        containers:
          - image: gcr.io/k8s-staging-test-infra/image-builder:v20251215-d7853fe2a6
            command:
              - /run.sh
            args:
              - --project=k8s-staging-images
              - --scratch-bucket=gs://k8s-staging-images-gcb
              - --env-passthrough=PULL_BASE_REF
              - --gcb-config=hack/prow/image/kubernetes-gvisor/cloudbuild.yaml
              - .
            env:
              - name: LOG_TO_STDOUT
                value: "y"
