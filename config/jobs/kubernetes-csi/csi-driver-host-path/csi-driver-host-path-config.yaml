# generated by gen-jobs.sh, do not edit manually

presubmits:
  kubernetes-csi/csi-driver-host-path:
  - name: pull-sig-storage-csi-driver-host-path-1-13-on-kubernetes-1-13
    # Experimental job, explicitly needs to be started with /test.
    # This can be enabled once the components have the necessary configuration
    # for this combination of deployment+Kubernetes.
    always_run: false
    decorate: true
    skip_report: false
    skip_branches: []
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-kind-volume-mounts: "true"
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        # We pick some version for which there are pre-built images for kind.
        # Update only when the newer version is known to not cause issues,
        # otherwise presubmit jobs may start to fail for reasons that are
        # unrelated to the PR. Testing against the latest Kubernetes is covered
        # by periodic jobs (see https://k8s-testgrid.appspot.com/sig-storage-csi#Summary).
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "1.13.3"
        - name: CSI_PROW_KUBERNETES_DEPLOYMENT
          value: "1.13"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
        resources:
          requests:
            cpu: 2000m
  - name: pull-sig-storage-csi-driver-host-path-deployment-1-13-on-kubernetes-1-13
    # Experimental job, explicitly needs to be started with /test.
    # This can be enabled once the components have the necessary configuration
    # for this combination of deployment+Kubernetes.
    always_run: false
    decorate: true
    skip_report: false
    skip_branches: []
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-kind-volume-mounts: "true"
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        # We pick some version for which there are pre-built images for kind.
        # Update only when the newer version is known to not cause issues,
        # otherwise presubmit jobs may start to fail for reasons that are
        # unrelated to the PR. Testing against the latest Kubernetes is covered
        # by periodic jobs (see https://k8s-testgrid.appspot.com/sig-storage-csi#Summary).
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "1.13.3"
        - name: CSI_PROW_KUBERNETES_DEPLOYMENT
          value: "1.13"
        # use unmodified deployment
        - name: CSI_PROW_BUILD_JOB
          value: "false"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
        resources:
          requests:
            cpu: 2000m
  - name: pull-sig-storage-csi-driver-host-path-1-13-on-kubernetes-1-14
    # Experimental job, explicitly needs to be started with /test.
    # This can be enabled once the components have the necessary configuration
    # for this combination of deployment+Kubernetes.
    always_run: false
    decorate: true
    skip_report: false
    skip_branches: []
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-kind-volume-mounts: "true"
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        # We pick some version for which there are pre-built images for kind.
        # Update only when the newer version is known to not cause issues,
        # otherwise presubmit jobs may start to fail for reasons that are
        # unrelated to the PR. Testing against the latest Kubernetes is covered
        # by periodic jobs (see https://k8s-testgrid.appspot.com/sig-storage-csi#Summary).
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "1.14.0"
        - name: CSI_PROW_KUBERNETES_DEPLOYMENT
          value: "1.13"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
        resources:
          requests:
            cpu: 2000m
  - name: pull-sig-storage-csi-driver-host-path-deployment-1-13-on-kubernetes-1-14
    # Experimental job, explicitly needs to be started with /test.
    # This can be enabled once the components have the necessary configuration
    # for this combination of deployment+Kubernetes.
    always_run: false
    decorate: true
    skip_report: false
    skip_branches: []
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-kind-volume-mounts: "true"
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        # We pick some version for which there are pre-built images for kind.
        # Update only when the newer version is known to not cause issues,
        # otherwise presubmit jobs may start to fail for reasons that are
        # unrelated to the PR. Testing against the latest Kubernetes is covered
        # by periodic jobs (see https://k8s-testgrid.appspot.com/sig-storage-csi#Summary).
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "1.14.0"
        - name: CSI_PROW_KUBERNETES_DEPLOYMENT
          value: "1.13"
        # use unmodified deployment
        - name: CSI_PROW_BUILD_JOB
          value: "false"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
        resources:
          requests:
            cpu: 2000m
  - name: pull-sig-storage-csi-driver-host-path-1-13-on-kubernetes-master
    # Experimental job, explicitly needs to be started with /test.
    # This cannot be enabled by default because there's always the risk
    # that something changes in master which breaks the pre-merge check.
    always_run: false
    decorate: true
    skip_report: false
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-bazel-remote-cache-enabled: "true"
      preset-kind-volume-mounts: "true"
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "latest"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
      resources:
        requests:
          # these are both a bit below peak usage during build
          # this is mostly for building kubernetes
          memory: "9000Mi"
          # during the tests more like 3-20m is used
          cpu: 2000m
  - name: pull-sig-storage-csi-driver-host-path-1-14-on-kubernetes-1-13
    # Experimental job, explicitly needs to be started with /test.
    # This can be enabled once the components have the necessary configuration
    # for this combination of deployment+Kubernetes.
    always_run: false
    decorate: true
    skip_report: false
    skip_branches: []
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-kind-volume-mounts: "true"
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        # We pick some version for which there are pre-built images for kind.
        # Update only when the newer version is known to not cause issues,
        # otherwise presubmit jobs may start to fail for reasons that are
        # unrelated to the PR. Testing against the latest Kubernetes is covered
        # by periodic jobs (see https://k8s-testgrid.appspot.com/sig-storage-csi#Summary).
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "1.13.3"
        - name: CSI_PROW_KUBERNETES_DEPLOYMENT
          value: "1.14"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
        resources:
          requests:
            cpu: 2000m
  - name: pull-sig-storage-csi-driver-host-path-deployment-1-14-on-kubernetes-1-13
    # Experimental job, explicitly needs to be started with /test.
    # This can be enabled once the components have the necessary configuration
    # for this combination of deployment+Kubernetes.
    always_run: false
    decorate: true
    skip_report: false
    skip_branches: []
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-kind-volume-mounts: "true"
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        # We pick some version for which there are pre-built images for kind.
        # Update only when the newer version is known to not cause issues,
        # otherwise presubmit jobs may start to fail for reasons that are
        # unrelated to the PR. Testing against the latest Kubernetes is covered
        # by periodic jobs (see https://k8s-testgrid.appspot.com/sig-storage-csi#Summary).
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "1.13.3"
        - name: CSI_PROW_KUBERNETES_DEPLOYMENT
          value: "1.14"
        # use unmodified deployment
        - name: CSI_PROW_BUILD_JOB
          value: "false"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
        resources:
          requests:
            cpu: 2000m
  - name: pull-sig-storage-csi-driver-host-path-1-14-on-kubernetes-1-14
    # Experimental job, explicitly needs to be started with /test.
    # This can be enabled once the components have the necessary configuration
    # for this combination of deployment+Kubernetes.
    always_run: false
    decorate: true
    skip_report: false
    skip_branches: []
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-kind-volume-mounts: "true"
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        # We pick some version for which there are pre-built images for kind.
        # Update only when the newer version is known to not cause issues,
        # otherwise presubmit jobs may start to fail for reasons that are
        # unrelated to the PR. Testing against the latest Kubernetes is covered
        # by periodic jobs (see https://k8s-testgrid.appspot.com/sig-storage-csi#Summary).
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "1.14.0"
        - name: CSI_PROW_KUBERNETES_DEPLOYMENT
          value: "1.14"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
        resources:
          requests:
            cpu: 2000m
  - name: pull-sig-storage-csi-driver-host-path-deployment-1-14-on-kubernetes-1-14
    # Experimental job, explicitly needs to be started with /test.
    # This can be enabled once the components have the necessary configuration
    # for this combination of deployment+Kubernetes.
    always_run: false
    decorate: true
    skip_report: false
    skip_branches: []
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-kind-volume-mounts: "true"
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        # We pick some version for which there are pre-built images for kind.
        # Update only when the newer version is known to not cause issues,
        # otherwise presubmit jobs may start to fail for reasons that are
        # unrelated to the PR. Testing against the latest Kubernetes is covered
        # by periodic jobs (see https://k8s-testgrid.appspot.com/sig-storage-csi#Summary).
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "1.14.0"
        - name: CSI_PROW_KUBERNETES_DEPLOYMENT
          value: "1.14"
        # use unmodified deployment
        - name: CSI_PROW_BUILD_JOB
          value: "false"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
        resources:
          requests:
            cpu: 2000m
  - name: pull-sig-storage-csi-driver-host-path-1-14-on-kubernetes-master
    # Experimental job, explicitly needs to be started with /test.
    # This cannot be enabled by default because there's always the risk
    # that something changes in master which breaks the pre-merge check.
    always_run: false
    decorate: true
    skip_report: false
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-bazel-remote-cache-enabled: "true"
      preset-kind-volume-mounts: "true"
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "latest"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
      resources:
        requests:
          # these are both a bit below peak usage during build
          # this is mostly for building kubernetes
          memory: "9000Mi"
          # during the tests more like 3-20m is used
          cpu: 2000m

periodics:
- interval: 6h
  name: ci-kubernetes-csi-1-13-on-kubernetes-1-13
  decorate: true
  extra_refs:
  # TODO: replace with kubernetes-csi/csi-driver-host-path master once branch prow is merged
  - org: pohly
    repo: csi-driver-host-path
    base_ref: prow
  labels:
    preset-service-account: "true"
    preset-dind-enabled: "true"
    preset-bazel-remote-cache-enabled: "false"
    preset-kind-volume-mounts: "true"
  spec:
    containers:
    # We need this image because it has Docker in Docker and go.
    - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
      command:
      - runner.sh
      args:
      - ./.prow.sh
      env:
      - name: CSI_PROW_KUBERNETES_VERSION
        value: "release-1.13"
      - name: CSI_PROW_BUILD_JOB
        value: "false"
      - name: CSI_PROW_DEPLOYMENT
        value: "kubernetes-1.13"
      # docker-in-docker needs privileged mode
      securityContext:
        privileged: true
        resources:
          requests:
            cpu: 2000m
- interval: 6h
  name: ci-kubernetes-csi-1-13-on-kubernetes-1-14
  decorate: true
  extra_refs:
  # TODO: replace with kubernetes-csi/csi-driver-host-path master once branch prow is merged
  - org: pohly
    repo: csi-driver-host-path
    base_ref: prow
  labels:
    preset-service-account: "true"
    preset-dind-enabled: "true"
    preset-bazel-remote-cache-enabled: "false"
    preset-kind-volume-mounts: "true"
  spec:
    containers:
    # We need this image because it has Docker in Docker and go.
    - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
      command:
      - runner.sh
      args:
      - ./.prow.sh
      env:
      - name: CSI_PROW_KUBERNETES_VERSION
        value: "release-1.14"
      - name: CSI_PROW_BUILD_JOB
        value: "false"
      - name: CSI_PROW_DEPLOYMENT
        value: "kubernetes-1.13"
      # docker-in-docker needs privileged mode
      securityContext:
        privileged: true
        resources:
          requests:
            cpu: 2000m
- interval: 6h
  name: ci-kubernetes-csi-1-13-on-kubernetes-master
  decorate: true
  extra_refs:
  # TODO: replace with kubernetes-csi/csi-driver-host-path master once branch prow is merged
  - org: pohly
    repo: csi-driver-host-path
    base_ref: prow
  labels:
    preset-service-account: "true"
    preset-dind-enabled: "true"
    preset-bazel-remote-cache-enabled: "true"
    preset-kind-volume-mounts: "true"
  spec:
    containers:
    # We need this image because it has Docker in Docker and go.
    - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
      command:
      - runner.sh
      args:
      - ./.prow.sh
      env:
      - name: CSI_PROW_KUBERNETES_VERSION
        value: "latest"
      - name: CSI_PROW_BUILD_JOB
        value: "false"
      - name: CSI_PROW_DEPLOYMENT
        value: "kubernetes-1.13"
      # docker-in-docker needs privileged mode
      securityContext:
        privileged: true
        resources:
          requests:
            cpu: 2000m
- interval: 6h
  name: ci-kubernetes-csi-1-14-on-kubernetes-1-13
  decorate: true
  extra_refs:
  # TODO: replace with kubernetes-csi/csi-driver-host-path master once branch prow is merged
  - org: pohly
    repo: csi-driver-host-path
    base_ref: prow
  labels:
    preset-service-account: "true"
    preset-dind-enabled: "true"
    preset-bazel-remote-cache-enabled: "false"
    preset-kind-volume-mounts: "true"
  spec:
    containers:
    # We need this image because it has Docker in Docker and go.
    - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
      command:
      - runner.sh
      args:
      - ./.prow.sh
      env:
      - name: CSI_PROW_KUBERNETES_VERSION
        value: "release-1.13"
      - name: CSI_PROW_BUILD_JOB
        value: "false"
      - name: CSI_PROW_DEPLOYMENT
        value: "kubernetes-1.14"
      # docker-in-docker needs privileged mode
      securityContext:
        privileged: true
        resources:
          requests:
            cpu: 2000m
- interval: 6h
  name: ci-kubernetes-csi-1-14-on-kubernetes-1-14
  decorate: true
  extra_refs:
  # TODO: replace with kubernetes-csi/csi-driver-host-path master once branch prow is merged
  - org: pohly
    repo: csi-driver-host-path
    base_ref: prow
  labels:
    preset-service-account: "true"
    preset-dind-enabled: "true"
    preset-bazel-remote-cache-enabled: "false"
    preset-kind-volume-mounts: "true"
  spec:
    containers:
    # We need this image because it has Docker in Docker and go.
    - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
      command:
      - runner.sh
      args:
      - ./.prow.sh
      env:
      - name: CSI_PROW_KUBERNETES_VERSION
        value: "release-1.14"
      - name: CSI_PROW_BUILD_JOB
        value: "false"
      - name: CSI_PROW_DEPLOYMENT
        value: "kubernetes-1.14"
      # docker-in-docker needs privileged mode
      securityContext:
        privileged: true
        resources:
          requests:
            cpu: 2000m
- interval: 6h
  name: ci-kubernetes-csi-1-14-on-kubernetes-master
  decorate: true
  extra_refs:
  # TODO: replace with kubernetes-csi/csi-driver-host-path master once branch prow is merged
  - org: pohly
    repo: csi-driver-host-path
    base_ref: prow
  labels:
    preset-service-account: "true"
    preset-dind-enabled: "true"
    preset-bazel-remote-cache-enabled: "true"
    preset-kind-volume-mounts: "true"
  spec:
    containers:
    # We need this image because it has Docker in Docker and go.
    - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
      command:
      - runner.sh
      args:
      - ./.prow.sh
      env:
      - name: CSI_PROW_KUBERNETES_VERSION
        value: "latest"
      - name: CSI_PROW_BUILD_JOB
        value: "false"
      - name: CSI_PROW_DEPLOYMENT
        value: "kubernetes-1.14"
      # docker-in-docker needs privileged mode
      securityContext:
        privileged: true
        resources:
          requests:
            cpu: 2000m
- interval: 6h
  name: ci-kubernetes-csi-canary-on-kubernetes-1-13
  decorate: true
  extra_refs:
  # TODO: replace with kubernetes-csi/csi-driver-host-path master once branch prow is merged
  - org: pohly
    repo: csi-driver-host-path
    base_ref: prow
  labels:
    preset-service-account: "true"
    preset-dind-enabled: "true"
    preset-bazel-remote-cache-enabled: "true"
    preset-kind-volume-mounts: "true"
  spec:
    containers:
    # We need this image because it has Docker in Docker and go.
    - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
      command:
      - runner.sh
      args:
      - ./.prow.sh
      env:
      - name: CSI_PROW_KUBERNETES_VERSION
        value: "1.13"
      - name: CSI_PROW_BUILD_JOB
        value: "false"
      - name: CSI_PROW_DEPLOYMENT
        value: "kubernetes-1.14"
      # docker-in-docker needs privileged mode
      securityContext:
        privileged: true
        resources:
          requests:
            cpu: 2000m
- interval: 6h
  name: ci-kubernetes-csi-canary-on-kubernetes-1-14
  decorate: true
  extra_refs:
  # TODO: replace with kubernetes-csi/csi-driver-host-path master once branch prow is merged
  - org: pohly
    repo: csi-driver-host-path
    base_ref: prow
  labels:
    preset-service-account: "true"
    preset-dind-enabled: "true"
    preset-bazel-remote-cache-enabled: "true"
    preset-kind-volume-mounts: "true"
  spec:
    containers:
    # We need this image because it has Docker in Docker and go.
    - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
      command:
      - runner.sh
      args:
      - ./.prow.sh
      env:
      - name: CSI_PROW_KUBERNETES_VERSION
        value: "1.14"
      - name: CSI_PROW_BUILD_JOB
        value: "false"
      - name: CSI_PROW_DEPLOYMENT
        value: "kubernetes-1.14"
      # docker-in-docker needs privileged mode
      securityContext:
        privileged: true
        resources:
          requests:
            cpu: 2000m
- interval: 6h
  name: ci-kubernetes-csi-canary-on-kubernetes-master
  decorate: true
  extra_refs:
  # TODO: replace with kubernetes-csi/csi-driver-host-path master once branch prow is merged
  - org: pohly
    repo: csi-driver-host-path
    base_ref: prow
  labels:
    preset-service-account: "true"
    preset-dind-enabled: "true"
    preset-bazel-remote-cache-enabled: "true"
    preset-kind-volume-mounts: "true"
  spec:
    containers:
    # We need this image because it has Docker in Docker and go.
    - image: gcr.io/k8s-testimages/kubekins-e2e:v20190329-811f7954b-master
      command:
      - runner.sh
      args:
      - ./.prow.sh
      env:
      - name: CSI_PROW_KUBERNETES_VERSION
        value: "latest"
      - name: CSI_PROW_BUILD_JOB
        value: "false"
      - name: CSI_PROW_DEPLOYMENT
        value: "kubernetes-1.14"
      # docker-in-docker needs privileged mode
      securityContext:
        privileged: true
        resources:
          requests:
            cpu: 2000m
