# generated by gen-jobs.sh, do not edit manually

presubmits:
  kubernetes-csi/node-driver-registrar:
  - name: pull-kubernetes-csi-node-driver-registrar-1-17-on-kubernetes-1-17
    always_run: true
    optional: false
    decorate: true
    skip_report: false
    skip_branches: ["^(release-1.0)$"]
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-kind-volume-mounts: "true"
    annotations:
      testgrid-dashboards: sig-storage-csi-node-driver-registrar
      testgrid-tab-name: 1-17-on-kubernetes-1-17
      description: Kubernetes-CSI pull job in repo node-driver-registrar for non-alpha tests, using deployment 1.17 on Kubernetes 1.17
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20201201-1941b8f-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        # We pick some version for which there are pre-built images for kind.
        # Update only when the newer version is known to not cause issues,
        # otherwise presubmit jobs may start to fail for reasons that are
        # unrelated to the PR. Testing against the latest Kubernetes is covered
        # by periodic jobs (see https://k8s-testgrid.appspot.com/sig-storage-csi-ci#Summary).
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "1.17.0"
        - name: CSI_PROW_KUBERNETES_DEPLOYMENT
          value: "1.17"
        - name: CSI_PROW_DRIVER_VERSION
          value: "v1.4.0"
        - name: CSI_SNAPSHOTTER_VERSION
          value: "v3.0.2"
        - name: CSI_PROW_TESTS
          value: "sanity serial parallel"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
        resources:
          requests:
            cpu: 2000m
  - name: pull-kubernetes-csi-node-driver-registrar-1-17-on-kubernetes-master
    # Explicitly needs to be started with /test.
    # This cannot be enabled by default because there's always the risk
    # that something changes in master which breaks the pre-merge check.
    always_run: false
    optional: true
    decorate: true
    skip_report: false
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-bazel-remote-cache-enabled: "true"
      preset-kind-volume-mounts: "true"
    annotations:
      testgrid-dashboards: sig-storage-csi-node-driver-registrar
      testgrid-tab-name: 1-17-on-kubernetes-master
      description: Kubernetes-CSI pull job in repo node-driver-registrar for non-alpha tests, using deployment 1.17 on Kubernetes master
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20201201-1941b8f-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "latest"
        - name: CSI_PROW_DRIVER_VERSION
          value: "v1.4.0"
        - name: CSI_SNAPSHOTTER_VERSION
          value: "master"
        - name: CSI_PROW_TESTS
          value: "sanity serial parallel"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
      resources:
        requests:
          # these are both a bit below peak usage during build
          # this is mostly for building kubernetes
          memory: "9000Mi"
          # during the tests more like 3-20m is used
          cpu: 2000m
  - name: pull-kubernetes-csi-node-driver-registrar-1-18-on-kubernetes-1-18
    always_run: true
    optional: false
    decorate: true
    skip_report: false
    skip_branches: ["^(release-1.0)$"]
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-kind-volume-mounts: "true"
    annotations:
      testgrid-dashboards: sig-storage-csi-node-driver-registrar
      testgrid-tab-name: 1-18-on-kubernetes-1-18
      description: Kubernetes-CSI pull job in repo node-driver-registrar for non-alpha tests, using deployment 1.18 on Kubernetes 1.18
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20201201-1941b8f-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        # We pick some version for which there are pre-built images for kind.
        # Update only when the newer version is known to not cause issues,
        # otherwise presubmit jobs may start to fail for reasons that are
        # unrelated to the PR. Testing against the latest Kubernetes is covered
        # by periodic jobs (see https://k8s-testgrid.appspot.com/sig-storage-csi-ci#Summary).
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "1.18.0"
        - name: CSI_PROW_KUBERNETES_DEPLOYMENT
          value: "1.18"
        - name: CSI_PROW_DRIVER_VERSION
          value: "v1.4.0"
        - name: CSI_SNAPSHOTTER_VERSION
          value: "v3.0.2"
        - name: CSI_PROW_TESTS
          value: "sanity serial parallel"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
        resources:
          requests:
            cpu: 2000m
  - name: pull-kubernetes-csi-node-driver-registrar-1-18-on-kubernetes-master
    # Explicitly needs to be started with /test.
    # This cannot be enabled by default because there's always the risk
    # that something changes in master which breaks the pre-merge check.
    always_run: false
    optional: true
    decorate: true
    skip_report: false
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-bazel-remote-cache-enabled: "true"
      preset-kind-volume-mounts: "true"
    annotations:
      testgrid-dashboards: sig-storage-csi-node-driver-registrar
      testgrid-tab-name: 1-18-on-kubernetes-master
      description: Kubernetes-CSI pull job in repo node-driver-registrar for non-alpha tests, using deployment 1.18 on Kubernetes master
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20201201-1941b8f-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "latest"
        - name: CSI_PROW_DRIVER_VERSION
          value: "v1.4.0"
        - name: CSI_SNAPSHOTTER_VERSION
          value: "master"
        - name: CSI_PROW_TESTS
          value: "sanity serial parallel"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
      resources:
        requests:
          # these are both a bit below peak usage during build
          # this is mostly for building kubernetes
          memory: "9000Mi"
          # during the tests more like 3-20m is used
          cpu: 2000m
  - name: pull-kubernetes-csi-node-driver-registrar-1-19-on-kubernetes-1-19
    always_run: true
    optional: true
    decorate: true
    skip_report: false
    skip_branches: ["^(release-1.0)$"]
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-kind-volume-mounts: "true"
    annotations:
      testgrid-dashboards: sig-storage-csi-node-driver-registrar
      testgrid-tab-name: 1-19-on-kubernetes-1-19
      description: Kubernetes-CSI pull job in repo node-driver-registrar for non-alpha tests, using deployment 1.19 on Kubernetes 1.19
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20201201-1941b8f-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        # We pick some version for which there are pre-built images for kind.
        # Update only when the newer version is known to not cause issues,
        # otherwise presubmit jobs may start to fail for reasons that are
        # unrelated to the PR. Testing against the latest Kubernetes is covered
        # by periodic jobs (see https://k8s-testgrid.appspot.com/sig-storage-csi-ci#Summary).
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "1.19.1"
        - name: CSI_PROW_KUBERNETES_DEPLOYMENT
          value: "1.19"
        - name: CSI_PROW_DRIVER_VERSION
          value: "v1.4.0"
        - name: CSI_SNAPSHOTTER_VERSION
          value: "v3.0.2"
        - name: CSI_PROW_TESTS
          value: "sanity serial parallel"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
        resources:
          requests:
            cpu: 2000m
  - name: pull-kubernetes-csi-node-driver-registrar-1-19-on-kubernetes-master
    # Explicitly needs to be started with /test.
    # This cannot be enabled by default because there's always the risk
    # that something changes in master which breaks the pre-merge check.
    always_run: false
    optional: true
    decorate: true
    skip_report: false
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-bazel-remote-cache-enabled: "true"
      preset-kind-volume-mounts: "true"
    annotations:
      testgrid-dashboards: sig-storage-csi-node-driver-registrar
      testgrid-tab-name: 1-19-on-kubernetes-master
      description: Kubernetes-CSI pull job in repo node-driver-registrar for non-alpha tests, using deployment 1.19 on Kubernetes master
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20201201-1941b8f-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "latest"
        - name: CSI_PROW_DRIVER_VERSION
          value: "v1.4.0"
        - name: CSI_SNAPSHOTTER_VERSION
          value: "master"
        - name: CSI_PROW_TESTS
          value: "sanity serial parallel"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
      resources:
        requests:
          # these are both a bit below peak usage during build
          # this is mostly for building kubernetes
          memory: "9000Mi"
          # during the tests more like 3-20m is used
          cpu: 2000m
  - name: pull-kubernetes-csi-node-driver-registrar-alpha-1-19-on-kubernetes-1-19
    always_run: false
    optional: true
    decorate: true
    skip_report: false
    skip_branches: ["^(release-1.0)$"]
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-kind-volume-mounts: "true"
    annotations:
      testgrid-dashboards: sig-storage-csi-node-driver-registrar
      testgrid-tab-name: alpha-1-19-on-kubernetes-1-19
      description: Kubernetes-CSI pull job in repo node-driver-registrar for alpha tests, using deployment 1.19 on Kubernetes 1.19
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20201201-1941b8f-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        # We pick some version for which there are pre-built images for kind.
        # Update only when the newer version is known to not cause issues,
        # otherwise presubmit jobs may start to fail for reasons that are
        # unrelated to the PR. Testing against the latest Kubernetes is covered
        # by periodic jobs (see https://k8s-testgrid.appspot.com/sig-storage-csi-ci#Summary).
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "1.19.1"
        - name: CSI_PROW_KUBERNETES_DEPLOYMENT
          value: "1.19"
        - name: CSI_PROW_DRIVER_VERSION
          value: "v1.4.0"
        - name: CSI_SNAPSHOTTER_VERSION
          value: "v3.0.2"
        - name: CSI_PROW_TESTS
          value: "serial-alpha parallel-alpha"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
        resources:
          requests:
            cpu: 2000m
  - name: pull-kubernetes-csi-node-driver-registrar-alpha-1-19-on-kubernetes-master
    # Explicitly needs to be started with /test.
    # This cannot be enabled by default because there's always the risk
    # that something changes in master which breaks the pre-merge check.
    always_run: false
    optional: true
    decorate: true
    skip_report: false
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-bazel-remote-cache-enabled: "true"
      preset-kind-volume-mounts: "true"
    annotations:
      testgrid-dashboards: sig-storage-csi-node-driver-registrar
      testgrid-tab-name: alpha-1-19-on-kubernetes-master
      description: Kubernetes-CSI pull job in repo node-driver-registrar for alpha tests, using deployment 1.19 on Kubernetes master
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20201201-1941b8f-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        - name: CSI_PROW_KUBERNETES_VERSION
          value: "latest"
        - name: CSI_PROW_DRIVER_VERSION
          value: "v1.4.0"
        - name: CSI_SNAPSHOTTER_VERSION
          value: "master"
        - name: CSI_PROW_TESTS
          value: "serial-alpha parallel-alpha"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
      resources:
        requests:
          # these are both a bit below peak usage during build
          # this is mostly for building kubernetes
          memory: "9000Mi"
          # during the tests more like 3-20m is used
          cpu: 2000m
  - name: pull-kubernetes-csi-node-driver-registrar-unit
    always_run: true
    decorate: true
    skip_report: false
    skip_branches: ["^(release-1.0)$"]
    labels:
      preset-service-account: "true"
      preset-dind-enabled: "true"
      preset-bazel-remote-cache-enabled: "true"
      preset-kind-volume-mounts: "true"
    annotations:
      testgrid-dashboards: sig-storage-csi-node-driver-registrar
      testgrid-tab-name: unit
      description: Kubernetes-CSI pull job in repo node-driver-registrar for unit tests
    spec:
      containers:
      # We need this image because it has Docker in Docker and go.
      - image: gcr.io/k8s-testimages/kubekins-e2e:v20201201-1941b8f-master
        command:
        - runner.sh
        args:
        - ./.prow.sh
        env:
        - name: CSI_PROW_TESTS
          value: "unit"
        # docker-in-docker needs privileged mode
        securityContext:
          privileged: true
      resources:
        requests:
          # these are both a bit below peak usage during build
          # this is mostly for building kubernetes
          memory: "9000Mi"
          # during the tests more like 3-20m is used
          cpu: 2000m
