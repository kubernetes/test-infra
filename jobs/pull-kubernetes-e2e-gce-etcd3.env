# Force to use container-vm.
KUBE_NODE_OS_DISTRIBUTION=debian

# Panic if anything mutates a shared informer cache
ENABLE_CACHE_MUTATION_DETECTOR=true

# Enable apps/v1beta2/statefulsets
# TODO (juntee): remove this before v1.8 release
RUNTIME_CONFIG=apps/v1beta2,apps/v1beta2/statefulsets=true

