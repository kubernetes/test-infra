# In contrast to the containerd image-config.yaml, this config
# tests only one OS. Please keep it that way, some jobs depend on it
# to avoid running the same test multiple times per job.
images:
  fedora:
    image_family: fedora-coreos-stable
    project: fedora-coreos-cloud
    metadata: "user-data</home/prow/go/src/k8s.io/test-infra/jobs/e2e_node/crio/crio.ign"
