kind: ConfigMap
apiVersion: v1
metadata:
  name: prometheus-${PROJECT}
  labels:
    app: prometheus
    project: ${PROJECT}
data:
  prometheus-config.yml: "Content of the prometheus config comes here."
